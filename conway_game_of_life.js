/*
 * 生成第N代
 * 放大数组范围,可能开拓新的地盘
 * 扫描原数组，每个位置的'气',判断在新位置的存活
 * 裁剪新数组<=是否固定范围？
 */
function getGeneration(cells,generations,clip){
  //input cells: array of arrays 

  var rows=cells.length;
  var cols=cells[0].length;

}
